<h1>トップページ</h1>
<div>
  <%= link_to '投稿する', new_item_path %>
</div>

<% @items.each do |item| %>
<div>
  <%= link_to item.title, edit_item_path(item.id) %>
</div>
<div>
  <%= image_tag  item.image, size: '300x400' %>
</div>
<div>
  <%= item.concept %>
</div>
<% end %>